{
  "name": "hublo",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Hublo — a macOS-style web desktop to manage your space on a Linux server, over SSH.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web install && npm --prefix web run build && rm -rf public && cp -r web/dist public"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^10.0.0",
    "@fastify/static": "^8.0.4",
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.2.1",
    "mysql2": "^3.22.5",
    "pg": "^8.22.0",
    "ssh2": "^1.16.0"
  }
}
